// Generated by CoffeeScript 1.7.1
ng.module('uiDropkick.directives').directive('ngDropkick', [
  'dropkick', function(model) {
    var dropkick, postLink, preLink;
    preLink = function(scope, elem, attrs, ctrl) {
      var $options, $select;
      elem.addClass('hideSelect');
      $select = elem;
      $options = $select.find('option');
      return console.log(elem.find('option'));
    };
    postLink = function(scope, elem, attrs, ctrl) {
      var $options, $select;
      elem.addClass('hideSelect');
      $select = angular.element(elem);
      $options = $select.find('option');
      return console.log(elem.find('option'));
    };
    dropkick = {
      restrict: 'A',
      scope: true,
      controller: 'dropkickCtrl',
      compile: function() {
        return {
          pre: preLink,
          post: postLink
        };
      }
    };
    return dropkick;
  }
]);
